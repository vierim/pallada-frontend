import { TopSection, MiddleSection, BottomSection } from './components';

export const Header = () => {
  return (
    <header>
      <TopSection />
      <MiddleSection />
      <BottomSection />
    </header>
  );
};
